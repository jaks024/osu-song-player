<Window x:Class="osu_song_player.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:osu_song_player"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Name="ctrlGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button Name="ctrlPlayBtn" Margin="5">Play</Button>
            <Button Name="ctrlPauseBtn" Grid.Column="1" Margin="5">Pause</Button>
            <Button Name="ctrlStopBtn" Grid.Column="2" Margin="5">Stop</Button>

            <Grid Grid.Column="3" VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="25"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="1" TextAlignment="Center" VerticalAlignment="Bottom">
                    song name - artist
                </TextBlock>

                <Label Grid.Row="1" HorizontalAlignment="Stretch" HorizontalContentAlignment="Right" Content="2:22"/>
                <Slider Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Value="2"></Slider>
                <Label Grid.Row="1" Grid.Column="2" Content="5:00"/>
            </Grid>
        </Grid>

        <Grid Name="centralGrid" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="25"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>

            <DockPanel Margin="5 0">
                <Label Content="Playlists" Width="55" HorizontalAlignment="Left"/>
                <Label Content="0" HorizontalAlignment="Right"/>
            </DockPanel>
            <ListBox Name="playlistListBox" Grid.Row="1" Margin="5 0" HorizontalContentAlignment="Stretch">
                <ListBoxItem>
                    <Label Content="My Favourites"/>
                </ListBoxItem>
                <ListBoxItem >
                    <Label Content="My Favourites"/>
                </ListBoxItem>
            </ListBox>
            <StackPanel Orientation="Horizontal" Grid.Row="2">
                <Button Width="50" Margin="5">
                    Delete
                </Button>
                <Button Width="50" Margin="5">
                    Rename
                </Button>
            </StackPanel>

            <DockPanel Grid.Column="1">
                <Label Content="Playlist name"/>
                <Label Content="500 Songs" Width="100" HorizontalAlignment="Right" HorizontalContentAlignment="Right"/>
            </DockPanel>

            <ListBox Name="songListBox" Grid.Column="1" Grid.Row="1" HorizontalContentAlignment="Stretch" ItemsSource="{Binding songs}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <Label Content="{Binding Order, Mode=OneWay}"/>
                            <Label DockPanel.Dock="Right" Grid.Column="2" Content="{Binding Length, Mode=OneWay}"/>
                            <Label Grid.Column="1" Content="{Binding Name, Mode=OneWay}"/>
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                <Button Width="50" Margin="0 5 5 5">
                    Delete
                </Button>
                <Button Width="50" Margin="5 5">
                    Refresh
                </Button>
            </StackPanel>

        </Grid>


        <StackPanel Name="footerStackPanel" Grid.Row="2" Orientation="Horizontal">
            <Button Name="selectPathBtn" Width="50" Margin="5" Click="SelectPathBtn_Click">Select</Button>
            <TextBlock Name="selectedPathTextBlock" Grid.Column="1" Margin="5" VerticalAlignment="Center">
                osu! song folder: C:\Users\Documents\GitHub\osu-song-player
            </TextBlock>

        </StackPanel>
    </Grid>
</Window>
